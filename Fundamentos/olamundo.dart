main() {
  print("Olá, Mundo!");

  /*
    OBSERVAÇÕES:
      Linguagem fortemente tipada;
      Os blocos de códigos são definidos por chaves {};
      Multiparadigma (suporta mais de um paradigma de programação);
      Baseada em clsses e objetos (POO);
      A sintaxe é baseada na linguagem C;
      Usada com o framework flutter para o desenvolvimento de interface de usuário.
  */
}
